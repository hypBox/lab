import { AppConfig } from "../../types"

const appConfig: AppConfig = {
  title: "hypBox Lab",
  slogan: "hypBox - Personalized e-Learning",
  video: {
    completedPercentage: 0.85,
  },
}

export default appConfig
